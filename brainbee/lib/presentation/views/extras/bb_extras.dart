import 'package:flutter/material.dart';

class BBExtras extends StatelessWidget {
  const BBExtras({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
