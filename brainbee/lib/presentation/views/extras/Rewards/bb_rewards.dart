import 'package:brainbee/core/utils/bb_text.dart';
import 'package:flutter/material.dart';

class BbRewards extends StatelessWidget {
  const BbRewards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: BBText(data: "Rewards")));
  }
}
