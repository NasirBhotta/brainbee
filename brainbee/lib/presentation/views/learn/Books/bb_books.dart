import 'package:flutter/material.dart';

class BBBooks extends StatelessWidget {
  const BBBooks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
