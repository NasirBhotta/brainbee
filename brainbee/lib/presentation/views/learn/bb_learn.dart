import 'package:flutter/material.dart';

class BbLearn extends StatelessWidget {
  const BbLearn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
